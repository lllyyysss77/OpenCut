interface:
  display_name: "OpenCut"
  short_description: "Repo-specific patterns and workflows for OpenCut"
  default_prompt: "Use the OpenCut repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true